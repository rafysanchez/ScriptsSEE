var _saldoReprogramado = (function () {

    var newURL = window.location.protocol + "//" + window.location.host + "/";

    //function _salvar(form) {

    //    $.ajax({
    //        cache: false,
    //        url: '../SalvarSaldoProgramado',
    //        data: { saldoExercicioAnterior: form },
    //        type: 'POST',
    //        datatype: 'JSON',
    //        contentType: 'application/x-www-form-urlencoded; charset=utf-8',
    //        success: function (data) {
    //            if (data != null && data != undefined) {
    //                var status = data[0].Status;
    //                if (status == "Sucesso") {
    //                    mensagemAlerta(data);
    //                    prestacaoContas.atualizarConteudoAbas();
    //                } else {
    //                    mensagemAlerta(data);
    //                }
    //            }
    //        },
    //        error: function (jqXHR, txtStatus, errorThrown) {
    //            mensagemAlerta.ErroAjax('Ocorreu um erro durante o processo' + errorThrown);
    //        }

    //    });
    //}


    //function _excluir(id) {

    //    $.ajax({
    //        cache: false,
    //        url: '../ExcluirSaldoReprogramado',
    //        data: { id: id },
    //        type: 'POST',
    //        datatype: 'JSON',
    //        contentType: 'application/x-www-form-urlencoded; charset=utf-8',
    //        success: function (data) {
    //            if (data != null && data != undefined) {
    //                var status = data[0].Status;
    //                if (status == "Sucesso") {
    //                    mensagemAlerta(data);
    //                    prestacaoContas.atualizarConteudoAbas();
    //                } else {
    //                    mensagemAlerta(data);
    //                }
    //            }
    //        },
    //        error: function (jqXHR, txtStatus, errorThrown) {
    //            mensagemAlerta.ErroAjax('Ocorreu um erro durante o processo' + errorThrown);
    //        }

    //    });
    //}


    return {
        //salvar: function (form) {
        //    _salvar(form);
        //},
        //excluir: function (id) {
        //    _excluir(id);
        //}

    }


})();